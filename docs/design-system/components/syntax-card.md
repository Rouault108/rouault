# Syntax Card

## 文書の目的

本書は、`ui-syntax-card` の公開契約、状態モデル、アクセシビリティ、視覚契約、および現行実装に残る未強制事項を整理するものです。

`ui-syntax-card` は、構文シグネチャと補足説明を分離し、API リファレンスや言語仕様書に近い読書体験を提供するコンポーネントです。単にコード片を囲う枠ではなく、**何を見出しとして提示するか**、**どのシグネチャをコピー対象とするか**、**詳細セクションが存在しない場合にどのように縮退するか**を公開契約として固定します。

本コンポーネントは `ui-syntax-section` と組み合わせて使うことを前提としており、構文の表面形と補足情報を同一カード上で秩序立てて提示します。Rouault における syntax card は、読み手が本文から API 記述へ一時的に移動しても集中を失わないことを重視します。したがって、本コンポーネントの契約は、**情報の切り分けの明快さ**と、**「没入して読む」ことのできるデザイン**の維持を両立する方向で定義します。

---

## 適用範囲

本書は、`ui-syntax-card` の**公開契約**を定義します。対象は次のとおりです。

- 公開入力（property / attribute / slot）
- 派生状態
- 見出し、コピー、詳細領域に関する表示契約
- DOM / Accessibility
- 環境別の振る舞い
- `ui-syntax-section` および `ui-copy-button` との連携契約
- 境界条件
- Storybook における契約確認点
- 不正構成に対する開発時診断方針

本書は、将来構想、拡張候補、実装メモ、移行計画そのものは対象としません。必要な場合は別文書で管理します。

一方で、本書は次の事項を対象外とします。

- シンタックスハイライト自体の実装
- 言語ごとの字句解析や構文解析
- コードブロック全体のテーマ切り替え
- `ui-copy-button` の内部仕様そのもの
- `ui-syntax-section` の本文内容の妥当性判断
- API ドキュメント全体の章構成や情報設計
- デザインシステム横断で定義すべき Reduced Motion 契約
- カード内部に公開するヘッダー拡張アクション面（permalink、原典リンク、補助操作 slot など）
- 意味整合的な言語 API への移行設計
- コピー値の明示 override 機能
- `identifier` と `name` の分離
- 節ラベルの推奨語彙、推奨順序、推奨本文文法などの補助レイヤ
- 多重シグネチャ対応
- 詳細セクションの折りたたみ
- `kind` の列挙固定

これらは上位レイヤ、横断契約、別コンポーネント、または別ガイドラインの責務です。

---

## コンポーネントの定義

`ui-syntax-card` は、**単一の代表シグネチャ**と、その補足説明節群を提示する参照カードです。

本コンポーネントは、構文の候補を比較する場ではありません。overload 群、複数言語の並列表記、複数シグネチャの比較表示は対象外とし、必要な場合は上位レイヤまたは別コンポーネントで扱います。

本コンポーネントの責務には、ヘッダーの描画、任意の種別タグの表示、カード主題見出しの階層制御、単一シグネチャの提示、コピー可能性の判定、詳細セクションの有無に応じたレイアウト切り替えを含みます。

一方で、どの言語識別子をどの見た目へ対応付けるか、シグネチャのシンタックスハイライト、詳細本文の意味内容の妥当性判断、節順序の自動決定は責務に含めません。

`name` は**表示上の主題名**です。`kind` は**自由テキストの補助ラベル**です。`lang` / `data-lang` は**外部スタイリングや統合のための opaque hook**であり、本コンポーネント自身はその語彙を列挙管理せず、意味解釈もしません。

---

## 公開契約

`ui-syntax-card` は、`kind`、`name`、`lang`、`headingLevel` を公開入力として扱います。スロットは `signature` スロットと既定スロットを持ちます。

公開契約上、`ui-syntax-card` の正規形は**代表シグネチャを持つこと**です。`signature` を欠いた描画が実装上成立し得るとしても、それは縮退であり、正規利用とはしません。

`kind` は任意入力です。空文字列の場合、種別タグは描画に参加しません。`kind` の語彙は列挙固定ではなく、自由テキストとして扱います。

`name` はカード見出しおよびコピー文脈の基準となる主題名です。実装上は空文字列でも描画が継続し得ますが、公開契約上の正規利用では空文字列にしてはなりません（SHOULD NOT）。

`lang` は公開 property 名としては `lang` ですが、HTML 属性面では `data-lang` を用います。これは自然言語を表す HTML の `lang` 属性ではなく、構文表示対象の言語識別子です。値は前後空白を除去し、小文字化したうえでホストの `data-lang` 属性へ反映します。空文字列の場合、`data-lang` 属性は除去されます。

`data-lang` の値は opaque token として扱います。`ui-syntax-card` 自身は値集合を列挙管理せず、妥当性検証や意味解釈も行いません。利用者は `data-lang` の語彙を本コンポーネントの列挙契約として扱ってはなりません（MUST NOT）。

`headingLevel` は、**カード主題見出し**の階層のみを制御します。`2` から `6` までを正規値として受理し、範囲外値、小数、非整数は公開契約上の無効値として `h4` へフォールバックします。`ui-syntax-section` 側のラベル階層には影響しません。

### スロット契約

`ui-syntax-card` の正規入力は、`signature` スロットに単一の `<pre>` を置き、既定スロットに `ui-syntax-section` を 0 件以上置く構成です。ここでいう「正規入力」は、執筆時およびレビュー時に満たすべき authoring contract を指します。

`signature` スロットの正規入力は単一の `<pre>` です。Markdown directive family からの正規出力も `<pre slot="signature" data-syntax-signature="true">...</pre>` とします。コピー値は `<code>` 子があればその `textContent` を優先し、無ければ `<pre>` 自身の `textContent` を使用します。

現行実装は、不正構成に対して例外を送出せず、安全側に縮退します。`signature` スロットに `<pre>` が 0 件、複数件、または空白のみのコードしか存在しない場合、コピーは無効化されます。既定スロットに `ui-syntax-section` 以外の要素が混在していても、それらは詳細領域の正規入力としては扱われません。

したがって、利用者は不正構成が実行時に自動修正または強制排除されることを期待してはなりません（MUST NOT）。本コンポーネントが保証するのは、正規入力の意味と、不正入力に対する安全側縮退までです。

#### Markdown authoring grammar との関係

`ui-syntax-card` の canonical authoring input は raw custom element ではなく、`syntax-card` / `syntax-signature` / `syntax-section` / `syntax-fields` / `syntax-field` から成る directive family です。

この family の最終出力契約は次のとおりです。

- `syntax-card` → `ui-syntax-card`
- `syntax-signature` → `pre[slot="signature"][data-syntax-signature="true"]`
- `syntax-section` → `ui-syntax-section`
- `syntax-fields` → `dl`
- `syntax-field` → `ui-syntax-field`

v1 では `syntax-signature` は通常 code block ではなく、syntax family の一部として扱います。したがって、signature 領域には Shiki ハイライトや standalone code surface を適用しません。

### 派生状態契約

`ui-syntax-card` は、公開入力とは別に次の派生状態を内部で導出します。

| 状態                 | 意味               | 導出条件                                                                       |
| -------------------- | ------------------ | ------------------------------------------------------------------------------ |
| `data-content-empty` | 詳細セクションなし | 既定スロットに `ui-syntax-section` が 0 件                                     |
| copy enabled         | コピー可能         | `signature` スロットに有効な `<pre>` がちょうど 1 件あり、抽出コードが空でない |
| copy disabled        | コピー不可         | 上記以外                                                                       |

`data-content-empty` は公開入力ではなく、実装が導出するホスト属性です。利用者はこの属性を手動で与えて状態を制御してはなりません（MUST NOT）。

### 動的更新契約

`ui-syntax-card` は初期描画時だけでなく、公開入力またはスロット内容の変更後も派生状態を再評価します。

| 変更対象             | 再評価される契約                   |
| -------------------- | ---------------------------------- |
| `name`               | コピー操作ラベル                   |
| `lang` / `data-lang` | 正規化済みホスト属性               |
| `signature` スロット | コピー可否、コピー値、コピーラベル |
| 既定スロット         | `data-content-empty` の有無        |

したがって、利用者は `ui-syntax-card` を静的な初期化専用コンポーネントとして扱う必要はありません。後から `name`、`lang`、`signature`、`ui-syntax-section` 群を差し替えた場合でも、契約上の状態は更新後の入力に追従します。

### 属性反映契約

| property       | attribute       | reflect  | 備考                                           |
| -------------- | --------------- | -------- | ---------------------------------------------- |
| `kind`         | `kind`          | なし     | Lit の通常 property / attribute 変換に従います |
| `name`         | `name`          | なし     | Lit の通常 property / attribute 変換に従います |
| `lang`         | `data-lang`     | 実質あり | 更新時に小文字化してホスト属性へ同期します     |
| `headingLevel` | `heading-level` | なし     | 範囲外値は描画時に `4` として解決します        |

とくに `lang` は通常の reflect 指定ではなく、更新時同期で `data-lang` へ反映されます。属性値の正規化は公開契約に含まれます。

---

## 状態モデル

`ui-syntax-card` の主要状態は、**シグネチャを持つか**、**詳細セクションを持つか**、**コピー可能か**、**見出しをどの階層で公開するか**によって読み分けます。

### 1. 基本状態

最小状態は、`name` を持ち、`signature` スロットに単一の `<pre>` を持ち、既定スロットに `ui-syntax-section` を持たない状態です。このときカードは、**シグネチャのみの参照カード**として振る舞います。

### 2. 種別タグ状態

`kind` が非空文字列である場合、ヘッダー左側に種別タグを描画します。`kind` は情報分類の補助であり、見出し本文の代替ではありません。`kind` を与えても `name` の責務は置き換わりません。

### 3. 見出しレベル状態

`headingLevel` は見出しの文書階層を制御します。`2` から `6` まではそのまま `h2` から `h6` へ対応付けます。範囲外値は `h4` へ縮退します。したがって、本コンポーネントは文書内での見出し階層を親文脈に合わせて調整できますが、**無効値をそのまま DOM へ反映することはしません**。

### 4. コピー状態

コピー状態は `signature` スロットの構成から導出します。

| 条件                                  | 状態          |
| ------------------------------------- | ------------- |
| `<pre>` が 1 件あり、抽出コードが非空 | copy enabled  |
| `<pre>` が 0 件                       | copy disabled |
| `<pre>` が複数件                      | copy disabled |
| `<pre>` は 1 件だがコードが空白のみ   | copy disabled |

copy disabled 時は `ui-copy-button` に `disabled` が伝播し、`aria-disabled="true"` と `tabindex="-1"` を持ちます。利用者は、コピー可能性を外部から直接上書きしてはなりません（MUST NOT）。

### 5. コンテンツ状態

既定スロットに `ui-syntax-section` が 1 件以上ある場合、詳細コンテンツ領域を表示します。0 件の場合は `data-content-empty` を付与し、詳細コンテンツ領域を非表示にします。このときシグネチャ領域下端の区切り線も除去され、**カードがシグネチャ単体の終端形**になります。

### 6. コピー操作の可視状態

コピー操作は常時露出しません。ホバーまたは `focus-within` のときに可視化します。ただし、`hover: none` かつ `pointer: coarse` 環境では、タッチ操作前提のため常時半透明で表示します。これは discoverability とノイズ抑制の折衷です。

---

## DOM / Accessibility

ルートは `:host` です。Shadow DOM 内部には、ヘッダー、シグネチャ領域、コンテンツ領域を持ちます。

```text
<ui-syntax-card>
  #shadow-root
    <header class="header">
      [span.kind-tag]
      <h2-h6 class="name">...</h2-h6>
      <ui-copy-button class="copy-action"></ui-copy-button>
    </header>
    <div class="signature-area">
      <slot name="signature"></slot>
    </div>
    <div class="content-area">
      <slot></slot>
    </div>
</ui-syntax-card>
```

`ui-syntax-card` 自体は、対話主体でも landmark でもありません。これは、カード全体を操作対象として扱うためではなく、**単一の代表シグネチャと補足節を静かに読むための視覚コンテナ**として扱うためです。対話要素はヘッダー内の `ui-copy-button` のみです。

また、`ui-syntax-section` のラベルはカード内部の補助ラベルであり、文書全体の見出しアウトラインへ参加することを目的としません。したがって、`headingLevel` はカード主題見出しにのみ作用し、節ラベルの階層制御には用いません。

### Accessibility 契約

アクセシビリティ上の重要点は次のとおりです。

- 見出しは必ず `h2` から `h6` のいずれかで描画します。
- `headingLevel` が不正でも、見出し要素は `h4` として維持します。
- コピー不能状態では、コピー操作は `disabled`、`aria-disabled="true"`、`tabindex="-1"` となり、キーボード到達性を持ちません。
- `ui-syntax-section` は内部で `section[aria-labelledby]` を用い、ラベルとセクション本文を関連付けます。
- `kind` は補助ラベルであり、見出し構造そのものではありません。

`ui-syntax-section` の見出し要素は `p.section-title` です。これは文書アウトライン上の追加見出しではなく、**カード内部の補助ラベル**として扱います。したがって、詳細セクションの階層を文書全体の見出し構造へ参加させることを目的としません。

### `ui-syntax-section` 連携契約

`ui-syntax-section` は、自由ラベルと本文を 1 セクションとして束ねる補助コンポーネントです。`ui-syntax-card` は、既定スロットに `ui-syntax-section` が存在するかどうかだけを見て、詳細領域の有無を決定します。

`ui-syntax-card` は `ui-syntax-section` の**入力順をそのまま表示順として保持**します。ラベル内容に基づく自動並べ替えや意味解釈は行いません。したがって、「パラメータ」「戻り値」「プロパティ」などの推奨順序が必要な場合、その順序を決める責務は利用側にあります。

`ui-syntax-card` は節種別の語彙を列挙管理しません。`ui-syntax-section` は自由ラベル節として扱い、ラベル語彙の統制や推奨順序は必要に応じて別ガイドラインで定義します。

`ui-syntax-card` は `ui-syntax-section` 本文の内部構造自体は規定しません。ただし、section 本文内で `ui-syntax-field` を用いる場合は、`<dl>` を section 本文内に置き、その `<dl>` 直下に `ui-syntax-field` を配置する構成を正規とします。`ui-syntax-card` 直下または `ui-syntax-section` 直下に、`<dl>` を介さず `ui-syntax-field` を直接置く構成は正規入力ではありません。

---

## Visual Contract

`ui-syntax-card` の視覚契約は、構文情報を**ヘッダー / シグネチャ / 詳細**の 3 層に分離して読む秩序を作ることにあります。

### 情報順位

- `kind` は分類情報です。
- `name` はカードの主題です。
- `signature` は主題の表面形です。
- `ui-syntax-section` 群は補足情報です。

この順序は視覚的にも保たれます。ヘッダーは種別と名称をまとめ、シグネチャは罫線で区切られた独立領域に置き、詳細は余白を広く取り、縦方向に積みます。

### レイアウト

ルートは block 要素です。幅は `--ui-syntax-card-breakout-width`、インライン余白は `--ui-syntax-card-breakout-margin` で調整できます。これにより、本文カラムより広いブレークアウト表示も可能です。

ヘッダーは横並びの flex で構成し、コピー操作は右端へ寄せます。シグネチャ領域は横スクロールを許可し、長いコードでもカード幅を破壊しません。詳細領域は縦方向の flex で、セクション間に一貫したギャップを取ります。

### 視覚仕様

- ルートは境界線と小さな角丸を持つカードです。
- `kind` は小さな大文字風タグとして表示します。
- `name` はモノスペース系の見出しとして表示します。
- コピー操作は通常時に隠れ、必要なときのみ現れます。
- `data-content-empty` 時は詳細領域を消し、シグネチャ領域の下罫線も消します。

以下に示す CSS Custom Properties を、`ui-syntax-card` の公開スタイル調整面として扱います。これ以外の内部 class 名、Shadow DOM 構造、要素配置は公開 API ではありません。

### 参照トークン

本コンポーネントは、主として次のトークンに依存します。

| 用途           | トークン                                          |
| -------------- | ------------------------------------------------- |
| カード幅       | `--ui-syntax-card-breakout-width`                 |
| カード余白     | `--ui-syntax-card-breakout-margin`                |
| 境界線         | `--border-width` / `--border-default`             |
| 背景色         | `--bg-default`                                    |
| 既定文字色     | `--fg-default`                                    |
| 控えめ文字色   | `--fg-muted`                                      |
| 角丸           | `--radius-sm`                                     |
| 余白           | `--space-*`                                       |
| フォントサイズ | `--text-xs` / `--text-sm` / `--text-base`         |
| フォント       | `--font-mono` / `--font-semibold` / `--font-bold` |
| 行高           | `--line-height-normal` / `--line-height-relaxed`  |
| トランジション | `--duration-normal` / `--ease-out`                |
| 非活性不透明度 | `--opacity-disabled` / `--opacity-link-touch`     |

`ui-syntax-card` は `::part(...)` を公開していません。したがって、外部スタイル拡張は CSS Custom Properties とホスト単位の配置調整に限定します。内部 class 名や Shadow DOM 構造に依存してはなりません（MUST NOT）。

---

## 環境別の振る舞い

### Touch / Coarse Pointer

`hover: none` かつ `pointer: coarse` 環境では、コピー操作を常時利用可能状態で表示します。これは、ホバーに依存した discoverability を避けるためです。

### Reduced Motion

`prefers-reduced-motion: reduce` 環境でも、本コンポーネントは動きそのものを意味伝達に使いません。コピー操作の可視化、シグネチャ提示、詳細領域の有無は、動きの有無にかかわらず同じ意味を保ちます。実装が視覚遷移を保持する場合でも、短時間で読み取りを阻害しない範囲にとどまらなければなりません（MUST）。

### Forced Colors

`forced-colors: active` 環境では、カード境界線、ヘッダー下罫線、シグネチャ下罫線、種別タグ、コピー操作の可視境界を `CanvasText` ベースで補強します。`ui-syntax-section` 側でもセクションタイトル色を `CanvasText` へフォールバックします。

### Print

`@media print` では、カード背景を透明化し、コピー操作を非表示にします。印刷時もシグネチャと詳細テキスト自体は残します。したがって、本コンポーネントは**印刷時に意味を保持する静的資料**として扱います。

---

## 関連契約

### `ui-copy-button` 連携契約

`ui-syntax-card` はコピー操作の UI と実コピー処理を `ui-copy-button` に委譲します。`ui-syntax-card` 側の責務は、**何をコピー対象とするかを決めること**に限定されます。

| 条件                  | `ui-copy-button` への反映                                      |
| --------------------- | -------------------------------------------------------------- |
| 有効なシグネチャ 1 件 | `value` に抽出コード、`label` に文脈付きコピーラベルを渡します |
| `name` が空           | 一般的なコピーラベルへ縮退します                               |
| `name` が非空         | `name` を含む文脈付きコピーラベルを生成します                  |
| コピー不能            | `disabled`、`aria-disabled="true"`、`tabindex="-1"`            |

コピーラベルの具体的な文言は、アクセシビリティ要件とローカライズ方針に従って将来調整され得ます。ただし、`name` がある場合に対象名を含む文脈付きラベルを生成すること、および `name` が空の場合に一般ラベルへ縮退することは公開契約として維持します。

### `ui-syntax-section` 連携契約

`ui-syntax-card` は既定スロットに `ui-syntax-section` が存在するかどうかだけを見て、詳細領域の有無を決定します。さらに、`ui-syntax-section` の**入力順をそのまま表示順として保持**します。ラベル内容に基づく自動並べ替えや意味解釈は行いません。

したがって、「パラメータ」「戻り値」「プロパティ」などの推奨順序が必要な場合、その順序を決める責務は利用側にあります。`ui-syntax-card` は `ui-syntax-section` の数を制限しませんが、過剰な分割で読書リズムを崩してはなりません。1 セクション 1 主題を原則とします。

`ui-syntax-card` は `ui-syntax-section` 本文の内部構造そのものは規定しません。ただし、section 本文内で `ui-syntax-field` を用いる場合は、`<dl>` を section 本文内に置き、その `<dl>` 直下に `ui-syntax-field` を配置する構成を正規とします。`ui-syntax-card` 直下または `ui-syntax-section` 直下に、`<dl>` を介さず `ui-syntax-field` を直接置く構成は正規入力ではありません。

### シグネチャ抽出契約

コピー対象は `signature` スロットの**直接子である単一の `<pre>`**から抽出します。ネストされた `<pre>`、ラッパー要素の内側に置かれた `<pre>`、複数の `<pre>` を許容する契約にはしていません。

また、改行は `\n` に正規化します。CRLF と CR は LF へ変換します。したがって、コピー値の行末形式に原文の改行コード保持を期待してはなりません（MUST NOT）。

ここでの契約は、**コピー対象の特定**にのみ作用します。`signature` スロット内容の表示可否とコピー可否は同一ではありません。`<pre>` 構造が不正な場合でも、スロット内容自体が表示されることはあり得ますが、コピー対象としては採用しません。

---

## 境界条件

### 1. `kind` なし

`kind` が空文字列の場合、種別タグは描画されません。見出しとコピー操作のみがヘッダーに残ります。

### 2. `name` あり、セクションなし

シグネチャのみのカードとして成立します。`data-content-empty` が付与され、詳細領域は非表示になります。

### 3. `ui-syntax-section` が 1 件のみ

単一セクションカードとして成立します。たとえば、「戻り値」のみ、または「プロパティ」のみの構成を許容します。

### 4. `headingLevel` 無効値

`headingLevel="9"` などの無効値は `h4` へフォールバックします。無効値エラーや描画中断は行いません。

### 5. `data-lang` 更新

`lang` / `data-lang` は更新時に小文字化してホスト属性へ再同期されます。たとえば `TS` は `ts` へ正規化されます。空文字列にした場合、属性は除去されます。

### 6. `signature` に `<pre>` がない

コピーは無効になります。`signature` スロット内容自体は表示され得ますが、コピー対象としては扱いません。

### 7. `signature` に `<pre>` が複数ある

コピーは無効になります。複数候補から自動選択は行いません。

### 8. `signature` のコードが空白のみ

コピーは無効になります。空文字列コピーは行いません。

### 9. 既定スロットに `ui-syntax-section` 以外のみがある

`data-content-empty` が付与され、詳細領域は非表示になります。既定スロットの任意ノードが自動的に可視化されることには依存できません。

### 10. `signature` に有効な `<pre>` 1 件とその他ノードが混在する

現行実装では、コピー判定は `<pre>` 要素だけを数えるため、有効な `<pre>` が 1 件であればコピーは有効になり得ます。一方で、`signature` スロットに与えたその他ノード自体はスロット描画上は残り得ます。したがって、`signature` 領域に `<pre>` 以外を混在させる構成は正規入力ではありませんが、実装上は表示とコピー可否が分離したまま成立し得ます。

### 11. 既定スロットに `ui-syntax-section` とその他ノードが混在する

現行実装では、既定スロットに `ui-syntax-section` が 1 件でも存在すれば `data-content-empty` は付与されません。そのため、`ui-syntax-section` 以外のノードも同じ既定スロットに混在している場合、詳細領域は表示状態となり、それらのノードもスロット描画上は残り得ます。これは正規入力ではありませんが、実装上は自動排除されません。

### 12. 印刷時

コピー操作は非表示になりますが、カード本体は残ります。インタラクションだけを除去し、情報は保持します。

### 13. `kind` と `name` がともに空

実装上は描画が継続し得ますが、公開契約上の正規利用とはしません。少なくとも `kind` または `name` のいずれかに、カードの意味核を与えることを前提とします。

### 14. `ui-syntax-section` の `label` が空

実装上は描画され得ますが、公開契約上は非推奨です。詳細セクションには、内容の主題を識別できる非空ラベルを与えることを前提とします。

### 15. `signature` なしで詳細のみが存在する

実装上はカードとして成立し得ますが、公開契約上の正規形は代表シグネチャを持つことです。詳細のみカードを常用してはなりません（SHOULD NOT）。

---

## Storybook 契約

各 Story は見本ではなく、**契約確認点**として扱います。将来変更時には、次の契約を維持します。

| Story                             | 固定する契約                                                                                                      |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `MethodWithMembersAndReturns`     | `kind`、見出し階層、複数 `ui-syntax-section`、コピー有効状態、入力順どおりのセクション順が成立すること            |
| `SingleSectionOnly`               | 単一セクションのみでも詳細領域が可視であること                                                                    |
| `EmptyContentContract`            | セクションが 0 件のとき `data-content-empty` が付与され、詳細領域が非表示となり、シグネチャ下罫線が除去されること |
| `HeadingLevelFallback`            | `2` は `h2`、無効値は `h4` にフォールバックすること                                                               |
| `LangAttribute`                   | `data-lang` がホスト属性へ反映され、更新時にも同期されること                                                      |
| `CopyFailureIsolation`            | `<pre>` 0 件 / 複数件 / 空コードのいずれでもコピーが無効になること                                                |
| `DefaultOnlyMembers`              | `ui-syntax-section` 1 件のみでも正しく表示されること                                                              |
| `ForcedColorsContract`            | `forced-colors` 用の視覚フォールバックが定義されていること                                                        |
| `PrintContract`                   | 印刷時にコピー操作が非表示となり、背景が透明化されること                                                          |
| `CopyDisabledInteractionContract` | コピー無効時に `disabled`、`aria-disabled`、`tabindex=-1` が成立すること                                          |

Storybook 契約上、`ui-syntax-card` は「Method」「Query」「Struct」「Component」などの内容類型に対応できますが、これらの語彙自体は自由入力です。列挙固定ではありません。

---

## 設計上の要点

`ui-syntax-card` の要点は、コードを見せることそのものではありません。**構文の代表形を 1 つに定め、その周囲の説明を節として整列させ、必要なときだけコピー操作を表に出すこと**にあります。

したがって、今後の変更でも次の 5 点は崩さない方がよいです。

1. 見出しは常に `h2` から `h6` のいずれかで出すこと。
2. コピー対象は単一シグネチャに限定し、曖昧な複数候補を自動解決しないこと。
3. 詳細領域の正規入力は `ui-syntax-section` に限定すること。
4. 印刷時はインタラクションのみを除去し、情報本体は残すこと。
5. 不正構成に対しては安全側に縮退しつつ、開発時には警告で誤用を発見しやすくすること。

とくに 5 点目は、公開契約を増やすためではなく、既存契約の保守可能性を高めるための原則です。`name` 空、`label` 空、既定スロット誤用、`signature` の `<pre>` 不備などに対して、実行時例外ではなく**開発時のみの静かな診断**を優先します。

---

## 現行実装で未対応または未強制の事項

本節は、現行の `syntax-card.ts`、`syntax-section.ts`、`syntax-card.stories.ts` を基準として、契約上は重要だが現時点では未強制、未拡張、または利用側規律に依存している事項を整理するものです。

### 1. `name` 非空の実行時強制

`name` はカード見出しの核ですが、現行実装は空文字列でも描画を継続します。空見出しに対する警告、例外、フォールバック文言はありません。したがって、**見出しの意味的妥当性は利用側規律に依存**しています。

### 2. 既定スロット入力の実行時警告

既定スロットの存在判定は `ui-syntax-section` の有無だけで行われますが、`ui-syntax-section` 以外の要素が入っている場合に警告は出しません。したがって、**不正入力が黙って非表示になる**可能性があります。

### 3. `signature` 構造違反の実行時警告

`signature` に `<pre>` がない、複数ある、空白のみである場合、コピーは安全側で無効化されます。しかし、違反内容の診断や警告は行いません。これは堅牢ではある一方で、**執筆時の構造ミスを早期に発見する仕組みは未実装**です。

### 4. `signature` スロット混在ノードの排除

現行実装は、`signature` スロットに割り当てられた要素のうち `<pre>` だけをコピー判定対象として数えます。そのため、有効な `<pre>` が 1 件であれば、`<pre>` 以外のノードが同じ `signature` スロットへ混在していてもコピーが有効なまま残り得ます。一方で、それらのノード自体を非表示にしたり警告したりする仕組みはありません。したがって、`signature` 領域における不正混在入力の排除や診断は未実装です。

### 5. 既定スロット混在ノードの排除

既定スロットの存在判定は `ui-syntax-section` の有無だけで行われます。このため、`ui-syntax-section` が 1 件でもあれば `data-content-empty` は解除され、同じ既定スロットに混在した任意ノードもスロット描画上は残り得ます。現行実装は、これらを自動排除したり警告したりしません。したがって、**既定スロットにおける typed slot の実行時強制は未実装**です。

### 6. `signature` 内の `<code>` 直子強制

現行実装は `pre.querySelector('code')` によって最初の子孫 `<code>` を取得します。したがって、`<pre><code>...</code></pre>` を標準構造としつつも、`<code>` が直子であること自体は実行時に強制していません。開発時警告もありません。したがって、`<code>` 直子構造の実行時強制は未実装です。

### 7. `data-lang` の意味利用

`lang` / `data-lang` は正規化されてホスト属性へ反映されますが、その値に応じてカード自身が見た目や振る舞いを切り替えるわけではありません。したがって、**言語識別子は現時点では主に外部スタイリングや将来拡張のためのフック**です。

### 8. ヘッダーアクション拡張

ヘッダー右端の操作は現行では `ui-copy-button` に固定されています。`ui-syntax-card` 自体は、複数アクション、補助リンク、field 単位パーマリンク、バージョン表示などの**内部ヘッダー拡張面**を公開していません。

一方で、これらの導線が文書全体として必要になる場合、それは `ui-syntax-card` 自体の責務ではなく、周辺ナビゲーション層、文書レイアウト層、または `ui-syntax-card` を包む上位コンテナで付与する事項です。したがって、現時点で未公開なのは「カード内部のヘッダー拡張面」であり、上位レイヤによる外付け導線までを否定するものではありません。

### 9. `ui-syntax-section` の見出し要素型固定

`ui-syntax-section` のラベルは `p` 要素で固定されており、文書階層に応じた見出しレベル調整は行いません。詳細セクションを文書アウトラインに参加させたい要件には未対応です。

### 10. Storybook の責務範囲

現行 Storybook はコピー可否、見出しフォールバック、空コンテンツ、`data-lang` 同期、印刷、forced-colors、セクション順保持を確認していますが、長大シグネチャ、極端に長い `name`、セクションラベル空文字、国際化文言切り替えに加えて、`signature` スロットへの混在ノード、既定スロットへの混在ノード、`<code>` が直子でない構造などは明示的には検証していません。

### 11. Reduced Motion の明示検証

本書では Reduced Motion 環境においても、動きに意味を依存させないことを契約として扱います。ただし、現行 Storybook には当該契約を単独で固定確認する専用ケースがありません。したがって、未対応なのは契約そのものではなく、Reduced Motion を独立に回帰確認する検証面です。

### 12. 本節の扱い

本節に記載した事項は、現行公開契約として依存してよい拡張ではありません。これらを正式契約へ昇格させる場合は、実装、Storybook、契約書の 3 点を同時に更新し、未強制のまま公開面へ昇格させません.
