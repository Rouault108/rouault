# Rouault

**Rouault（ルオー）** は、個人的なメモ（ノート）を閲覧するための専用Webアプリケーションです。
一般的なドキュメントサイトのように、サイドバー・メモ本文・目次（TOC）の **3カラム構成** でコンテンツを整理し、プライバシーを重視したセキュアな環境でナレッジベースにアクセスできます。

---

## 🎯 プロジェクトの目的

- **パーソナルナレッジマネジメント**: 音楽、文学、美術、計算機科学など多様なジャンルのメモを閲覧・整理
- **高アクセシビリティ**: WCAG 2.1 Level AA 準拠を目標
- **モダンブラウザ最適化**: 古いブラウザの互換性より機能性を優先
- **恒久的リンク**: コンテンツの各バージョンを永続的に参照可能

---

## 📐 サイト構成

### レスポンシブブレークポイント

| サイズ           | 幅                | カラム構成                                      |
| ---------------- | ----------------- | ----------------------------------------------- |
| **デスクトップ** | ≥768px            | 3カラム（サイドバー + メモ + TOC）              |
| **タブレット**   | 768px未満 〜 中間 | 2カラム（メモ + TOC）、サイドバーはボタンで開閉 |
| **モバイル**     | 小                | 1カラム + スティッキー要素                      |

### ページレイアウト

#### デスクトップ（3カラム構成）

```
┌─────────────────────────────────────────────────────────────────┐
│ [スキップリンク: メインコンテンツへスキップ]                       │
├─────────────────────────────────────────────────────────────────┤
│                     <header> ヘッダー                            │
│ [ロゴ]  [ジャンル一覧▼] [このサイトについて]    [🔍 検索] [テーマ] │
├────────────┬──────────────────────────────────┬─────────────────┤
│  <aside>   │           <main>                 │    <aside>      │
│ サイドバー  │          メモ本文                 │      TOC        │
│ (階層選択)  │                                  │  (自動階層化)    │
│ (タグ絞込) │                                  │                 │
├────────────┴──────────────────────────────────┴─────────────────┤
│                     <footer> フッター                            │
│                       © 著作権表記                               │
└─────────────────────────────────────────────────────────────────┘
```

#### タブレット（2カラム構成）

- **ヘッダー**: デスクトップと同様
- **サイドバー**: 非表示（左下にフローティングボタンで開閉）
- **メイン**: メモ本文
- **TOC**: 右側に表示

#### モバイル（1カラム＋スティッキー要素）

- **ヘッダー**: ロゴ + 検索アイコン + テーマボタン + ハンバーガーメニュー
- **スティッキーTOC**: ヘッダーの高さ分スクロール後に表示、現在のセクションタイトル + **円形の極小プログレスインジケータ**
- **サイドバーボタン**: 画面左下にフローティングアイコン
- **ホームアイコン**: スティッキーTOC左側に常時表示

---

### メモの構成

```markdown
[パンくずリスト] ← 5階層以上は中間を「...」で省略
[ドラフトアイコン（draft: true の場合）] [タイトル]
[説明]
[タグ（ジャンル）] ← High Density UI（高密度・小フォント）、色分け、クリックで一覧ページへ
[ライセンス表記（あれば）]
[作成日・更新日] ← CI/CDで自動更新（2024年1月21日 形式）
────────────────── （ボーダーライン）
[メモ本文] ← デフォルトフォントサイズ 14px
```

### タグページ

- canonical URL は **`/tags/<tag>/`**
- 単一タグ既定ビューとして `search-page` を利用し、検索語・追加タグ・並び順・タグ演算子が既定状態を外れたら **`/search?...`** へ遷移
- 結果は **リスト形式** で表示（タイトル + パスラベル + メタデータ）

### 404ページ

- カスタム404ページを用意

---

## 📁 コンテンツ管理

### ディレクトリ構造

```plaintext
content/
├── music/                      # 音楽ジャンル
│   ├── _config.json            # 並び順設定（継承なし、各ディレクトリ独立）
│   ├── classical/              # サブジャンル
│   │   ├── _config.json
│   │   ├── tchaikovsky/        # 作曲家サブジャンル
│   │   │   ├── _config.json
│   │   │   └── the-nutcracker.md
│   │   └── beethoven/
│   │       └── symphony-9.md
│   └── jazz/
│       └── jazz-theory.md
├── literature/                 # 文学ジャンル
├── art/                        # 美術ジャンル
├── computer-science/           # 計算機科学ジャンル
├── economics/                  # 経済学ジャンル
├── sociology/                  # 社会学ジャンル
├── political-science/          # 政治学ジャンル
├── law/                        # 法学ジャンル
└── mathematics/                # 数学ジャンル
```

### ファイル名規則

- **ケバブケース必須**（例: `the-nutcracker.md`）
- **日本語ファイル名は使用しない**（URLエンコード問題を回避）
- サイドバー表示名はフロントマターの `title` を使用

### メモのフロントマター

```yaml
---
title: '楽曲分析: くるみ割り人形'
description: 'チャイコフスキーのバレエ音楽を分析したメモ'
genre: # タグ一覧・色分け用（ディレクトリ構造とは独立）
  - music
  - classical
  - ballet
cover: '/images/covers/nutcracker.jpg' # OGP/メタ画像用
license: 'CC BY 4.0'
licenseNote: '© Original Author'
draft: false # true でドラフトアイコン表示（デフォルト: false）
---
```

### ディレクトリ vs タグ（genre）の関係

| 項目       | ディレクトリ構造                     | genre（フロントマター）     |
| ---------- | ------------------------------------ | --------------------------- |
| **用途**   | サイドバー分類、木構造ナビゲーション | タグ一覧ページ、色分け      |
| **整合性** | **不要**（独立して管理）             | —                           |
| **例**     | `music/classical/tchaikovsky/`       | `[music, analysis, ballet]` |

### 並び順設定（\_config.json）

```json
{
  "order": ["the-nutcracker.md", "swan-lake.md"]
}
```

- **継承なし**（各ディレクトリ独立）
- 指定がないファイル: **アルファベット順 → 日本語順**

### ドラフト機能

- `draft: true` を指定した場合:
  - メモタイトル前にドラフトアイコン（`aria-label="下書き"`）を表示
  - サイドバーのタイトル前にもドラフトアイコンを表示
- デフォルト: `false`

---

## 🔍 検索機能

| 項目           | 仕様                                                                                     |
| -------------- | ---------------------------------------------------------------------------------------- |
| **エンジン**   | [Pagefind](https://pagefind.app/)                                                        |
| **日本語対応** | `Intl.Segmenter` による分かち書き                                                        |
| **UI**         | ヘッダー右側に検索ボックス（モバイルではアイコンのみ）                                   |
| **URL**        | 汎用検索は `/search?...`、単一タグ既定ビューは `/tags/<tag>/`                            |
| **タグ連携**   | `genre` をタグとして扱い、複数タグは既定で **OR**、`tagMode=and` で **AND** に切り替える |

---

## 🎭 アイコン

| 用途                                       | 現行の実装                                                               |
| ------------------------------------------ | ------------------------------------------------------------------------ |
| 一般アイコン（コンテンツ・ナビゲーション） | `ui-icon` + [Iconify - Lucide](https://icon-sets.iconify.design/lucide/) |
| UI内部装飾（コンポーネント内蔵）           | **インライン SVG**                                                       |
| 技術ロゴ                                   | 現行コードでは未接続                                                     |

> **Note**: `ui-icon` は `src/icons/catalog.ts` にある Lucide 名だけを受け取り、内部で `iconify-icon` に委譲します。`ui-checkbox` のような固定 glyph は、自己完結性を優先して inline SVG を使います。Devicon / Simple Icons の記述は現時点の runtime には接続していません。

## UI

### 階層ナビゲーション (Tree View)

ユーザーのメンタルモデルと一致する、予測可能で堅牢なツリー構造を提供します。奇をてらった動的な再構成は行わず、標準的な挙動を最高品質で実装することで「静謐さ」を保ちます。

| 項目                 | 仕様                                                                                                                                      |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| **構造**             | 定番のネスト可能なツリー構造。                                                                                                            |
| **現在地の明示**     | 現在閲覧中のメモを自動的に展開し、視覚的にハイライト（Suble Active）する。                                                                |
| **状態保持**         | 展開/収縮の状態は `localStorage` に保持する。初回表示では「現在地へのパス」を必ず展開し、その上で保存済みの展開状態をマージして復元する。 |
| **オートスクロール** | ページロード時、現在のアクティブな項目が可視領域に含まれるよう **即座に（アニメーションなしで）** 表示位置を調整する。                    |
| **アニメーション**   | 展開/収縮は `duration-fast` (70ms) で即座に完了させる。                                                                                   |

### フィルター

| 項目               | 仕様                                                          |
| ------------------ | ------------------------------------------------------------- |
| **タグフィルター** | タグをクリックして絞り込み                                    |
| **複合条件**       | 検索語 + タグ絞り込み。複数タグは **OR / AND** を切り替え可能 |

### TOC（目次）

| 項目                 | 仕様                                         |
| -------------------- | -------------------------------------------- |
| **生成方法**         | Markdown の見出しから自動生成                |
| **開始レベル**       | レベル2から（レベル1はタイトル用に予約）     |
| **インデント**       | レベル2 < レベル3 < レベル4                  |
| **スティッキー表示** | ヘッダー高さ分スクロール後に表示（モバイル） |

---

## 📝 ジャンル別特殊機能

### 🎵 音楽（楽譜表示）

| 項目                 | 仕様                                                          |
| -------------------- | ------------------------------------------------------------- |
| **方式**             | LilyPond → SVG 変換                                           |
| **ビルド環境**       | GitHub Actions（Cloudflare Pages では不可）                   |
| **生成タイミング**   | ハッシュ比較で更新時のみ再生成                                |
| **バージョン管理**   | `.lilypond-version` ファイルで固定                            |
| **エラー処理**       | エラーメッセージを SVG として出力                             |
| **アクセシビリティ** | `<title>` と `<desc>` を SVG 内に埋め込み、独自記法で指定可能 |
| **対応範囲**         | クラシック音楽 + 現代音楽                                     |

`````markdown
<!-- メモ内での使用例 -->

````lilypond title="くるみ割り人形 冒頭" desc="チャイコフスキー作曲"
\relative c' {
  \time 3/4
  c4 e g | c2.
}
```　
````
`````

````

### 📚 文学（対訳表示）

| 項目                   | 仕様                                                                       |
| ---------------------- | -------------------------------------------------------------------------- |
| **マークアップ**       | 独自パーサー（MDX不使用）                                                  |
| **PC表示**             | ホバーで翻訳表示（ポップオーバー）、またはパラレル表示（右側ペインで同期） |
| **モバイル表示**       | ボトムシート表示（デフォルト）、インターリニア表示（設定により可）         |
| **関連コンポーネント** | `<ui-translation>` (Web Components)                                        |
| **アクセシビリティ**   | `aria-details` と `role="button"` を使用                                   |

```markdown
<!-- メモ内での使用例 -->

:::translation lang="fr"
Je pense, donc je suis.
:::translated lang="ja"
我思う、ゆえに我あり。
:::
```

### 💻 プログラミング（コードハイライト）

| 項目               | 仕様                                                                                   |
| ------------------ | -------------------------------------------------------------------------------------- |
| **エンジン**       | [Shiki](https://shiki.matsu.io/)（ビルド時生成のみ）                                   |
| **実行タイミング** | ビルド時生成                                                                           |
| **テーマ**         | Dual theme（`github-light` / `github-dark`）を `prefers-color-scheme` ベースで切り替え |
| **機能**           | シンタックスハイライト、行番号、行ハイライト、コピーボタン                             |
| **カスタム言語**   | Shiki のカスタム grammar 定義（未着手）                                                |

---

## 🔗 パーマリンク戦略

コンテンツの内容が変わっても参照先の内容が保証される**恒久的リンク（Permanent URL）**を提供。

### URL方式

| URLタイプ         | 形式               | 役割                                  |
| ----------------- | ------------------ | ------------------------------------- |
| **Canonical URL** | `/{slug}`          | 最新版の表示、SEO、シェア             |
| **Permanent URL** | `/archives/{hash}` | 引用、過去版参照（`robots: noindex`） |

### ハッシュ仕様

| 項目             | 仕様                                                              |
| ---------------- | ----------------------------------------------------------------- |
| **URL構造**      | `/archives/{hash}`                                                |
| **ハッシュ長**   | 12文字（SHA-256の先頭48bit）                                      |
| **ハッシュ形式** | 小文字の16進数                                                    |
| **ハッシュ対象** | 正規化後の Markdown 全体（Frontmatter + 本文、`updated_at` 除外） |

### 正規化処理

1. `updated_at` フィールドを除外
2. 改行コードを `LF` に統一
3. 末尾の空白を除去
4. ファイル末尾に改行を追加（POSIX準拠）
5. SHA-256を計算し、先頭12文字をハッシュとして使用

### アーキテクチャ

| コンポーネント                     | 役割                                                          |
| ---------------------------------- | ------------------------------------------------------------- |
| `config/permalinks.json`           | ハッシュ履歴とメタデータを管理                                |
| Cloudflare R2 (`rouault-archives`) | 正規化後 Markdown を保存                                      |
| Cloudflare Workers                 | `/archives/{hash}` へのアクセス時に R2 から取得・レンダリング |

### permalinks.json スキーマ

```json
{
  "currentHash": "a1b2c3d4e5f6",
  "history": [
    { "hash": "a1b2c3d4e5f6", "date": "2025-12-22", "message": "バージョン2.0" },
    { "hash": "f7e8d9c0b1a2", "date": "2025-11-01", "message": "初版公開" }
  ]
}
```

### 衝突対策

| ケース                           | 対処                                     |
| -------------------------------- | ---------------------------------------- |
| 新規コンテンツ                   | 新規ハッシュとして登録、R2にアップロード |
| 同一コンテンツ（Revert等）       | 既存ハッシュを使用（再アップロード不要） |
| 衝突（ハッシュ一致、内容不一致） | ハッシュを16文字に延長して再計算         |

### CI/CDフロー

```
main ブランチへの PR マージ
  ↓
GitHub Actions: アーカイブ生成ジョブ起動
  ↓
変更された記事を検出（git diff）
  ↓
各記事に対して:
  1. 正規化処理
  2. ハッシュ計算（12文字）
  3. permalinks.json で重複チェック
     ├─ 重複なし → R2 にアップロード、permalinks.json 更新
     └─ 重複あり → スキップ
  ↓
permalinks.json をコミット（自動）
```

### UI要件

| 要素                  | 仕様                                                       |
| --------------------- | ---------------------------------------------------------- |
| **デフォルト表示URL** | Permanent URL を優先提示、Canonical URL は補足             |
| **コピー機能**        | ワンクリックで Permanent URL をクリップボードにコピー      |
| **アーカイブ警告**    | 「これは過去のバージョンです。最新版はこちら」アラート表示 |

---

## 🛠 技術スタック

| カテゴリ                 | 技術                                                                      |
| ------------------------ | ------------------------------------------------------------------------- |
| **SSG**                  | [Eleventy](https://www.11ty.dev/)                                         |
| **コンポーネント**       | [Lit](https://lit.dev/)                                                   |
| **言語**                 | TypeScript                                                                |
| **コンテンツ**           | [Velite](https://velite.js.org/) (Markdown)                               |
| **検索**                 | [Pagefind](https://pagefind.app/)                                         |
| **ルーティング**         | Custom Router (SPA + View Transitions API)                                |
| **コードハイライト**     | [Shiki](https://shiki.matsu.io/)（ビルド時生成）                          |
| **楽譜レンダリング**     | [LilyPond](https://lilypond.org/) → SVG                                   |
| **ホスティング**         | Cloudflare Pages                                                          |
| **アーカイブストレージ** | Cloudflare R2                                                             |
| **CI/CD**                | GitHub Actions                                                            |
| **テスト**               | Web Test Runner (ロジック単体), Vitest + Storybook (UI), Playwright (E2E) |
| **リンター**             | ESLint + Prettier                                                         |

---

## 🏭 レンダリング戦略 (SSG/SSR)

### コンセプト: Hybrid SSG + Client Hydration

本プロジェクトでは **SSG (Static Site Generation)** を基本としつつ、`@lit-labs/ssr` を活用して Web Components をビルド時にプリレンダリングします。

### アーキテクチャ判断

1.  **`@lit-labs/ssr` の用途**
    - **SSG 目的で使用**: リクエスト毎の動的レンダリング（SSR）ではなく、Eleventy のビルドプロセス内で Lit コンポーネントを HTML 文字列に変換するために使用します。
    - **ハイドレーション**: クライアント側で Lit がロードされると、既存の Declarative Shadow DOM を「活性化」（Hydration）し、インタラクティブになります。

2.  **擬似的な ISR (Incremental Static Regeneration)**
    - Cloudflare Pages の **「Git プッシュごとの自動ビルド」** を活用します。
    - コンテンツ更新頻度が比較的低いため、動的な ISR（Next.js のようなオンデマンド再生成）ではなく、修正のたびに静的ファイルを全生成する SSG で十分と判断しました。
    - これにより、CDN エッジからの超高速配信（TTFB の最小化）と、最新コンテンツの維持を両立します。

3.  **SSR (リクエスト時生成) を採用しない理由**
    - 個人メモという性質上、ユーザーごとに異なる動的コンテンツが少ないため。
    - 検索機能（Pagefind）やナビゲーションはクライアントサイドで効率的に動作するため。

### レンダリングフロー

```mermaid
graph TD
    A[Markdown Content] -->|Eleventy Build| B(Static HTML Generation)
    C[Lit Components] -->|@lit-labs/ssr| B
    B --> D[Cloudflare Pages / CDN]
    D -->|Request| E[Browser]
    E -->|Declarative Shadow DOM| F[Fast FCP]
    G[Client JS] -->|Hydration| H[Interactive UI]
```

---

## 📂 プロジェクト構造

```plaintext
Rouault/
├── content/                     # コンテンツソース (Markdown)
│   ├── music/
│   │   └── classical/
│   │       └── tchaikovsky/
│   ├── literature/
│   ├── art/
│   ├── computer-science/
│   ├── economics/
│   ├── sociology/
│   ├── political-science/
│   ├── law/
│   └── mathematics/
├── config/
│   ├── permalinks.json          # パーマリンク履歴管理
│   └── tag-normalization.json   # タグ正規化マッピング
├── src/
│   ├── assets/
│   │   ├── css/                 # グローバルCSS、デザイントークン
│   │   └── images/
│   ├── components/
│   │   ├── ui/                  # 汎用UIコンポーネント
│   │   ├── layout/              # Header, Sidebar, TOC, Footer
│   │   └── content/             # Translation, Score
│   ├── layouts/                 # Eleventyレイアウト
│   ├── lib/
│   │   ├── router/              # SPAルーター
│   │   ├── search/              # 検索機能
│   │   ├── lilypond/            # 楽譜生成
│   │   └── translation/         # 対訳パーサー
│   └── client.ts
├── .github/
│   └── workflows/               # GitHub Actions
│       ├── build.yml            # ビルド・デプロイ
│       └── archive.yml          # アーカイブ生成
├── test/
│   ├── e2e/
│   └── unit/
└── dist/
```

---

## ♿ アクセシビリティ

### 目標

**WCAG 2.1 Level AA** 準拠

### 実装

| 機能                   | 実装方法                                               |
| ---------------------- | ------------------------------------------------------ |
| **スキップリンク**     | ページ先頭に「メインコンテンツへスキップ」リンク       |
| **ランドマーク**       | `<header>`, `<nav>`, `<main>`, `<aside>`, `<footer>`   |
| **セマンティックHTML** | 適切な見出し階層、リスト、テーブル構造                 |
| **ARIA属性**           | `aria-label`, `aria-describedby`, `aria-live`          |
| **フォーカス管理**     | 可視フォーカスリング、論理的なタブ順序                 |
| **カラーコントラスト** | 4.5:1 以上（通常テキスト）、3:1 以上（大きなテキスト） |
| **モーション配慮**     | `prefers-reduced-motion` 対応                          |
| **ドラフトアイコン**   | `aria-label="下書き"` を追加                           |

### 非実装

- **キーボードショートカット**: VoiceOver 等のスクリーンリーダーとのショートカット競合を避けるため非実装

---

## 🖨 印刷スタイル

| 項目           | 仕様                                |
| -------------- | ----------------------------------- |
| **表示要素**   | メモ本文のみ                        |
| **非表示要素** | サイドバー、TOC、ヘッダー、フッター |

---

## 🖼 OGP / Twitter Card

| 項目               | 仕様                                                |
| ------------------ | --------------------------------------------------- |
| **cover 指定あり** | `cover` フィールドの画像を使用                      |
| **cover 指定なし** | 白背景にタイトルを記載した画像を **ビルド時に生成** |

---

## 💻 開発ガイド

### 前提条件

- Node.js v22以上
- pnpm
- LilyPond（楽譜生成用、ローカル開発時）

### セットアップ

```bash
pnpm install
```

### 開発コマンド

```bash
# 開発サーバー
pnpm dev

# ビルド
pnpm build

# テスト
pnpm test             # test:unit + test:ssr + test:storybook
pnpm test:unit        # ロジック寄りの単体テスト
pnpm test:ssr         # SSR / build-time 契約テスト
pnpm test:storybook   # Lit コンポーネント / UI テスト
pnpm test:e2e         # E2Eテスト

# Storybook
pnpm storybook

# リント
pnpm lint
pnpm lint:fix
```

### テスト戦略

- `pnpm test:unit` は Web Test Runner を使い、`router`、`remark/rehype`、controller などのロジック寄りテストを担当します。
- `pnpm test:ssr` は SSR、serialization、Markdown / build-time 変換、static artifact shape を担当します。
- Lit の `@property` / `@state` / `@customElement` などのデコレータを使うコンポーネントは、現状の Web Test Runner 経路では安定して扱いません。
- Lit コンポーネントの状態・描画・interaction・a11y と browser-observable contract は `pnpm test:storybook` 側で検証します。
- 新しい Lit コンポーネントテストを追加する場合は、まず Storybook/Vitest 側に置くことを原則とします。

---

## 📅 日付フォーマット

| 項目         | 仕様                             |
| ------------ | -------------------------------- |
| **内部保存** | UTC                              |
| **表示時**   | JST に変換、`2024年1月21日` 形式 |
| **作成日**   | Git の最初のコミット日           |
| **更新日**   | Git の最新コミット日             |

---

## 📄 ライセンス

未定

---

## 🗺️ 実装計画

### 概要

本計画は、README.md に記載された仕様を基に、段階的に機能を実装するためのロードマップです。
各フェーズは独立してテスト・デプロイ可能な単位で構成されています。

---

### フェーズ 0: 基盤整備 ✅ (完了)

| タスク                                                  | 状態 |
| ------------------------------------------------------- | ---- |
| プロジェクト初期化 (pnpm, TypeScript)                   | ✅   |
| Eleventy セットアップ                                   | ✅   |
| Lit + Storybook 環境構築                                | ✅   |
| デザインシステム定義 (`tokens.css`, `design-system.md`) | ✅   |
| ESLint + Prettier 設定                                  | ✅   |
| Vitest 設定                                             | ✅   |
| Playwright 設定                                         | ✅   |

#### 完了済み UI コンポーネント

**目標**: アプリケーション全体で必要となる UI コンポーネントをすべて実装する。

| コンポーネント | 状態 |
| -------------- | ---- |
| `ui-button`    | ✅   |
| `ui-input`     | ✅   |
| `ui-card`      |      |
| `ui-tag`       |      |

#### フォーム要素

| コンポーネント | 説明                                           | 状態 |
| -------------- | ---------------------------------------------- | ---- |
| `ui-checkbox`  | チェックボックス（タスクリスト、フィルター用） |      |
| `ui-radio`     | ラジオボタン（単一選択）                       |      |
| `ui-toggle`    | トグルスイッチ（テーマ切替、設定用）           |      |
| `ui-dropdown`  | ドロップダウン選択                             |      |
| `ui-textarea`  | 複数行テキスト入力（将来の編集機能用）         |      |
| `ui-select`    | セレクトボックス                               |      |
| `ui-slider`    | スライダー                                     |      |
| `ui-switch`    | スイッチ                                       |      |
| `ui-range`     | レンジ                                         |      |
| `ui-meter`     | メーター                                       |      |
| `ui-rating`    | レーティング                                   |      |

#### ナビゲーション & アクション

| コンポーネント                 | 説明                                         | 状態 |
| ------------------------------ | -------------------------------------------- | ---- |
| `ui-icon-button`               | アイコンのみのボタン（検索、テーマ切替など） |      |
| `ui-link`                      | スタイル付きリンク                           |      |
| `ui-breadcrumb`                | パンくずリスト                               |      |
| `ui-pagination`                | ページネーション                             |      |
| `ui-tabs`                      | タブ切り替え                                 |      |
| `ui-menu` / `ui-dropdown-menu` | ドロップダウンメニュー（ヘッダーナビ用）     |      |

#### フィードバック & オーバーレイ

| コンポーネント | 説明                           | 状態 |
| -------------- | ------------------------------ | ---- |
| `ui-toast`     | トースト通知                   |      |
| `ui-modal`     | モーダルダイアログ             |      |
| `ui-popover`   | ポップオーバー                 |      |
| `ui-tooltip`   | ツールチップ                   |      |
| `ui-banner`    | バナー（アーカイブ警告など）   |      |
| `ui-spinner`   | ローディングスピナー           |      |
| `ui-skeleton`  | スケルトンローディング         |      |
| `ui-progress`  | プログレスバー（読書進捗など） |      |

#### タイポグラフィ / コンテンツ（Markdown 由来）

| コンポーネント  | 説明                                                     | 状態 |
| --------------- | -------------------------------------------------------- | ---- |
| `ui-code`       | インラインコード (`<code>`)                              |      |
| `pre[data-code-block]` | 静的コードブロック（シンタックスハイライト付き）   |      |
| `section[data-code-group]` | 静的コードグループ（native tab structure） |      |
| `ui-preview`    | コードブロックのプレビュー表示（code-groupの上位）       |      |
| `ui-file-tree`  | ディレクトリ構造のツリー表示（YAML形式で記述すると出力） |      |
| `ui-blockquote` | 引用ブロック                                             |      |
| `ui-callout`    | 注意書き、ヒント、警告などのコールアウト                 |      |
| `ui-table`      | テーブルスタイリング                                     |      |
| `ui-list`       | リスト (ul, ol) スタイリング                             |      |
| `ui-task-list`  | タスクリスト（チェックボックス付きリスト）               |      |
| `ui-heading`    | 見出しスタイリング (h1-h6)                               |      |
| `ui-divider`    | 水平区切り線                                             |      |
| `ui-kbd`        | キーボードショートカット表示                             |      |
| `ui-mark`       | ハイライトテキスト                                       |      |

#### レイアウト補助

| コンポーネント | 説明                                       | 状態 |
| -------------- | ------------------------------------------ | ---- |
| `ui-badge`     | バッジ（ドラフトアイコン、カウンターなど） |      |
| `ui-avatar`    | アバター（将来の著者表示用）               |      |

---

### フェーズ 1: コンテンツ基盤

**目標**: Markdown コンテンツを読み込み、基本的なページとして表示できる状態にする。

| タスク                                                                                                                 | 依存         | 状態 |
| ---------------------------------------------------------------------------------------------------------------------- | ------------ | ---- |
| Velite 設定・スキーマ定義                                                                                              | -            |      |
| フロントマター検証 (Zod)                                                                                               | Velite       |      |
| `content/` ディレクトリ構造のサンプル作成                                                                              | Velite       |      |
| `_config.json` による並び順制御                                                                                        | Velite       |      |
| Eleventy + Velite 統合                                                                                                 | 両方         |      |
| 基本レイアウトテンプレート (1カラム)                                                                                   | Eleventy     |      |
| Markdown → HTML 変換パイプライン（例えば`<pre><code>`を除く通常の`<code>`を`ui-code`に変換するrehypeプラグインを実装） | -            |      |
| remark/rehype プラグイン設定 (GFM, 数式, 絵文字)                                                                       | パイプライン |      |

---

### フェーズ 2: レイアウト構築

**目標**: 3カラムレイアウト（サイドバー + メイン + TOC）を実装し、レスポンシブ対応を行う。

| タスク                                     | 依存         | 状態 |
| ------------------------------------------ | ------------ | ---- |
| `layout-header` コンポーネント             | -            |      |
| `layout-sidebar` コンポーネント (階層選択) | Velite       |      |
| `layout-toc` コンポーネント (目次)         | -            |      |
| `layout-footer` コンポーネント             | -            |      |
| 3カラムグリッドレイアウト (CSS Grid)       | 全レイアウト |      |
| レスポンシブブレークポイント実装           | グリッド     |      |
| モバイル: スティッキー TOC                 | TOC          |      |
| モバイル: フローティングサイドバーボタン   | サイドバー   |      |
| スキップリンク                             | -            |      |

---

### フェーズ 3: ルーティング & ナビゲーション

**目標**: SPA ルーターを実装し、View Transitions API による滑らかなページ遷移を実現する。

| タスク                                | 依存                 | 状態 |
| ------------------------------------- | -------------------- | ---- |
| カスタムルーター設計                  | -                    | ✅   |
| `fetch` によるコンテンツ取得          | ルーター             | ✅   |
| `document.startViewTransition()` 統合 | ルーター             | ✅   |
| 履歴管理 (History API)                | ルーター             | ✅   |
| サイドバー ↔ メイン連携               | サイドバー, ルーター | ✅   |
| 階層選択の状態保持 (localStorage)     | サイドバー           | ✅   |

---

### フェーズ 4: 検索機能

**目標**: Pagefind を統合し、日本語対応の全文検索を実現する。

| タスク                                  | 依存       | 状態        |
| --------------------------------------- | ---------- | ----------- |
| Pagefind ビルド統合                     |            | ✅ 実装済み |
| `Intl.Segmenter` による日本語分かち書き | Pagefind   | ✅ 実装済み |
| タグフィルター連携 (OR 既定 / AND 切替) | 検索, タグ | ✅ 実装済み |
| 検索結果ページ (タグ一覧ページ兼用)     | 検索結果   | ✅ 実装済み |

---

### フェーズ 5: SSG + Lit SSR 統合

**目標**: `@lit-labs/ssr` を Eleventy ビルドに統合し、Web Components をプリレンダリングする。

| タスク                                   | 依存       | 状態            |
| ---------------------------------------- | ---------- | --------------- |
| `@lit-labs/ssr` セットアップ             | -          | ✅ 実装済み     |
| Eleventy Transform での Lit レンダリング | SSR        | ✅ 実装済み     |
| Declarative Shadow DOM 出力              | SSR        | ✅ 実装済み     |
| クライアントハイドレーション検証         | SSR        | ✅ 実装済み     |
| ビルドパフォーマンス最適化               | SSR 統合後 | ✅ 一部実装済み |

---

### フェーズ 6: ジャンル別特殊機能

**目標**: 音楽（楽譜）、文学（対訳）、プログラミング（コードハイライト）の専用機能を実装する。

#### 6-A: コードハイライト

| タスク                               | 依存  | 状態                 |
| ------------------------------------ | ----- | -------------------- |
| Shiki 統合 (ビルド時)                | -     | ✅ 実装済み          |
| Dual theme (light/dark) CSS 切り替え | Shiki | ✅ 実装済み          |
| 行番号・行ハイライト                 | Shiki | ✅ 実装済み          |
| PrismJS フォールバック               | -     | 廃止（Shiki に統一） |
| Twoslashの静的ビルド時生成設定       | -     | 未着手               |

#### 6-B: 楽譜表示 (LilyPond)

| タスク                              | 依存         | 状態 |
| ----------------------------------- | ------------ | ---- |
| GitHub Actions ワークフロー設計     | -            |      |
| LilyPond → SVG 変換スクリプト       | ワークフロー |      |
| ハッシュ比較による増分生成          | スクリプト   |      |
| SVG 内 `<title>`, `<desc>` 埋め込み | スクリプト   |      |
| エラー時の SVG 出力                 | スクリプト   |      |

#### 6-C: 対訳表示

| タスク                                    | 依存     | 状態 |
| ----------------------------------------- | -------- | ---- |
| 独自マークアップパーサー                  | -        |      |
| PC: ホバー翻訳ポップアップ (`ui-popover`) | パーサー |      |
| モバイル: インターリニア表示              | パーサー |      |
| 表示切替トグル (`ui-toggle`)              | 両モード |      |
| `lang` 属性対応                           | パーサー |      |

---

### フェーズ 7: パーマリンクシステム

**目標**: 恒久的リンク（Permanent URL）を実装し、コンテンツのバージョン管理を行う。

| タスク                                            | 依存       | 状態 |
| ------------------------------------------------- | ---------- | ---- |
| `permalinks.json` スキーマ設計                    | -          |      |
| 正規化処理 (LF, 空白除去, SHA-256)                | -          |      |
| Cloudflare R2 バケット設定                        | -          |      |
| GitHub Actions: アーカイブ生成ワークフロー        | R2, 正規化 |      |
| Cloudflare Workers: `/archives/{hash}` ハンドラー | R2         |      |
| UI: Permanent URL コピーボタン (`ui-copy-button`) | -          |      |
| UI: アーカイブ警告バナー (`ui-alert`)             | Workers    |      |

---

### フェーズ 8: デプロイ

**目標**: 本番環境にデプロイする。

| タスク                            | 依存  | 状態 |
| --------------------------------- | ----- | ---- |
| Cloudflare Pages プロジェクト作成 | -     |      |
| 自動デプロイ (GitHub 連携)        | Pages |      |
| 404 カスタムページ                | -     |      |
| OGP / Twitter Card 画像生成       | -     |      |

---

### フェーズ 9: アクセシビリティ & 品質保証

**目標**: WCAG 2.1 Level AA 準拠を検証し、E2E テストを整備する。

| タスク                                       | 依存             | 状態 |
| -------------------------------------------- | ---------------- | ---- |
| axe-core / Storybook a11y アドオンによる検証 | 全コンポーネント |      |
| フォーカス管理の検証                         | -                |      |
| カラーコントラスト検証                       | -                |      |
| `prefers-reduced-motion` 対応確認            | -                |      |
| スクリーンリーダーテスト (VoiceOver/NVDA)    | -                |      |
| Playwright E2E テストスイート作成            | 全フェーズ       |      |
| パフォーマンス監査 (Lighthouse)              | デプロイ後       |      |

---

### マイルストーン

| マイルストーン                   | 含まれるフェーズ | 目標                       |
| -------------------------------- | ---------------- | -------------------------- |
| **MVP (Minimum Viable Product)** | 0, 0.5, 1, 2, 3  | 基本的なメモ閲覧が可能     |
| **検索・SSR 対応**               | 4, 5             | 高速検索とプリレンダリング |
| **フル機能**                     | 6, 7             | 楽譜、対訳、パーマリンク   |
| **本番リリース**                 | 8, 9             | 認証、品質保証完了         |

---

### 進捗管理

- [x] フェーズ 0: 基盤整備
- [ ] フェーズ 0.5: 追加 UI コンポーネント
- [ ] フェーズ 1: コンテンツ基盤
- [ ] フェーズ 2: レイアウト構築
- [ ] フェーズ 3: ルーティング & ナビゲーション
- [ ] フェーズ 4: 検索機能
- [ ] フェーズ 5: SSG + Lit SSR 統合
- [ ] フェーズ 6: ジャンル別特殊機能
- [ ] フェーズ 7: パーマリンクシステム
- [ ] フェーズ 8: 認証 & デプロイ
- [ ] フェーズ 9: アクセシビリティ & 品質保証
````
